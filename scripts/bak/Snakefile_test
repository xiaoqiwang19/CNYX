import os
import yaml
import sys
from config import config  as  cg

ref=cg.CFG['database.hg19']['fasta']

print(ref)

#path=os.path.dirname(os.path.realpath(__file__))

#print(path)

print(workflow.snakefile)

SAMPLES=config['sample_all']
print(SAMPLES)

for each in SAMPLES:
    print(each,config[each])
